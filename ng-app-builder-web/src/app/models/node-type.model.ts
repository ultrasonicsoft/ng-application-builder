export enum NodeType {
    module = 'module',
    moduleWithRoute = 'moduleWithRoute',
    component = 'component',
    standaloneComponent ='standaloneComponent',
    service = 'service',
    pipe = 'pipe',
    directive = 'directive'
}